#include<stdio.h>

int main(){
     int a=3,b=-7; //if we use negative int, remainder will also be negative
     //remainder operation
     printf("the value of remainder, b divided by a is:\t %d", b%a); //% is modulus operator
    return 0;
}